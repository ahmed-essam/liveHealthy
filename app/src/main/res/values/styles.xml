<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:colorForeground">@android:color/primary_text_light</item>
        <item name="android:textColorPrimary">@color/textColorDark</item>
        <!-- color of menu overflow icon (three vertical dots) -->
        <item name="android:textColorSecondary">@color/colorAccent</item>

    </style>
    <style name="SpinnerTheme" parent="ThemeOverlay.AppCompat.Dark">

        <item name="colorAccent">@color/textColorDark</item>
    </style>

    <style name="baseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="editTextTheme" parent="baseTheme">
        <item name="android:textColor" >@android:color/primary_text_light</item>
    </style>

</resources>
